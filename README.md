# AstroDX-Collection-Generator
为AstroDX2.0以后的版本生成谱面收藏夹的工具。

## 使用方式
1. 将程序文件放在你需要作为收藏夹的文件夹目录中，如`.../1. maimai`。
2. 确保该目录下已存放好谱面文件夹。
3. 启动程序，程序会自动分析并生成`manifest.json`。
4. 完成后，将生成的文件移入`collections/[收藏夹目录]`。

可根据情况自行修改文件内容。
